version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - exhaustive
    - gocritic
    - gosec
    - loggercheck
    - misspell
    - nilerr
    - noctx
    - revive

formatters:
  enable:
    - gofumpt
    - goimports
